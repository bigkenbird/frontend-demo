import { IData } from '@twix/ix-lib-base';
export declare const vBindCreate: {
    created(el: HTMLElement, binding: IData): void;
    beforeMount(el: HTMLElement, binding: IData): void;
    mounted(el: HTMLElement, binding: IData): void;
};
