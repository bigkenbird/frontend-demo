/**
 * prepend - 把node放入parentNode下的第一個位置
 *
 * @param parentNode
 * @param node
 * @returns
 */
export declare function prependNode(parentNode: Node | HTMLElement, node: Node | HTMLElement): Node | HTMLElement | null;
