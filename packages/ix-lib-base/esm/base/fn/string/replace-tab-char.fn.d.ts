/**
 * 去掉字串中的tab字元
 *
 * @example
 * ```
 * replaceTabChar('\ttest') // 'test'
 * ```
 * @param str 替換字串
 * @param replacement 取代字串,預設為''
 * @returns 替換後字串
 * @author Clark Chen
 * @version 2021/03/10
 */
export declare function replaceTabChar(str: string, replacement?: string): string | null;
