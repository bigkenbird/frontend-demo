/**
 * 控制target元素以下(含target本身)的form防止瀏覽器出現未預期的原生行為(控制項)
 *
 * @param target
 * @returns
 * @author Clark Chen
 * @version 2021/03/10
 */
export declare function handleFormUnexpectedUI(target: HTMLElement): void;
