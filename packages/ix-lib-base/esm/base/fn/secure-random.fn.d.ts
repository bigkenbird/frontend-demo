/**
 * 整數範圍內 產生一個亂數
 * @example
 * ```
 * secureRandom(0, 100) // 隨機產生0~100的一個整數值
 * ```
 * @param min
 * @param max
 * @returns 會隨機產生指定範圍的一個整數值
 */
export declare function secureRandom(min: number, max: number): number;
